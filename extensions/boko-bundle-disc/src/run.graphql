query RunInput {
  cart {
    lines {
      id
      quantity
      bundle: attribute(key: "_boko_bundle") { value }
    }
  }
  discountNode {
    metafield(namespace: "$app", key: "function-configuration") { value }
  }
}
